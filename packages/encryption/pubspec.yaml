name: encryption
description: Cross-platform encryption layer with SQLCipher and OS keychain integration.
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.6
  pointycastle: ^3.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.7
  mocktail: ^1.0.4
